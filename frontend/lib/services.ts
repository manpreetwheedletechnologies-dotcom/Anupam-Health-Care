// Content moved to the database. Services are now managed from the
// admin dashboard (Admin → Services) and served via GET /services.
// See lib/api.ts (getServices, getServiceBySlug) and
// context/SiteDataContext.tsx for how components consume them.
// This file is intentionally left empty of exports.
export {};
